<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.user">
	<resultMap type="userVO" id="userResult">
		<result property="id" column="id"/>
		<result property="username" column="username"/>
		<result property="password" column="password"/>
		<result property="createdAt" column="created_at"/>
	</resultMap>
	
	<resultMap type="tokenVO" id="tokenResult">
		<result property="token" column="token"/>
		<result property="userId" column="user_id"/>
		<result property="createdAt" column="created_at"/>
	</resultMap>
	
	<select id="selectAllUserList" resultMap="userResult">
      <![CDATA[
         select * from user
      ]]>
	</select>
	
	<select id="loginByUser"  resultMap="userResult"   parameterType="userVO" >
	<![CDATA[
		select * from user	
		where username=#{username} and password=#{password}		
	]]>
	</select>
	
	<select id="getUserOne"  resultMap="userResult"   parameterType="Long" >
	<![CDATA[
		select * from user
		where id=#{id}		
	]]>
	</select>
	
	<insert id="insertUser"  parameterType="userVO" keyProperty="id" useGeneratedKeys="true">
		<![CDATA[
		 insert into user(username, password)
		 values(#{username}, #{password})
		]]>      
	</insert>
	
	<insert id="insertToken"  parameterType="tokenVO" useGeneratedKeys="true" keyProperty="token">
		<![CDATA[
		 insert into token(token, user_id)
		 values(#{token}, #{userId})
		]]>      
	</insert>
	
	<select id="selectToken"  parameterType="String" resultMap="tokenResult">
		<![CDATA[
			select * from token
			where token=#{token}
		]]>      
	</select>
</mapper>